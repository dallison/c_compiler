#! /bin/sh
# Tests skipped for davecc -target arm.

if ! test -f README.md; then
    echo "run from the c_testsuite directory." >&2
    exit 1
fi

# Same portability constraints as other DaveCC targets; extend as needed.

# The ARM interpreter currently faults while loading this broad POSIX test.
# Native ARM Linux coverage for these APIs lives in native_linux_smoke.cpp.
echo "tests/single-exec/00242.c"
