#! /bin/sh
# Tests skipped for davecc -target riscv.

if ! test -f README.md; then
    echo "run from the c_testsuite directory." >&2
    exit 1
fi

# No tests currently skipped for riscv: the full single-exec suite passes at
# O0/O1/O2.  Add entries here ("tests/single-exec/NNNNN.c") if a test must be
# skipped in the future.
