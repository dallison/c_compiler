# Execution tests that do not pass at -O2 on ARM.
#
# The suite runs every test at -O2.  A failure outside this list is a
# regression, and a test in the list that passes fails the suite too, so
# that fixing one forces its entry to go and the list cannot drift out of
# date.  A test marked "flaky" may do either, for one that does not reach
# the same result twice running; keep that marker rare.
#
# Nothing here is expected to stay: each entry is an -O2 bug to fix.
flaky 0298_standard_blocking_sync.cpp
